Server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/capstone5703?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=GMT
    username: root
    password: capstone5703

redis:
  host: localhost
  port: 6379
  max-wait: 30000
  max-active: 100
  max-idle: 20
  min-idle: 0
  timeout: 3000

mybatis:
  mapper-locations: classpath:/mapper/*.xml